return {
    -- "OmniSharp/omnisharp-vim",
    -- config = function()
    --     local lspconfig = require('lspconfig')

    --     lspconfig.omnisharp.setup({
    --         settings = {
    --         }
    --     })
    -- end
}
